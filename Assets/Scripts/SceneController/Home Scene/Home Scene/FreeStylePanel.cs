using System.Collections;
using System.Collections.Generic;
using UnityEngine;
namespace NailSalonGame
{
    public class FreeStylePanel : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
        public void GotoFreeStyleScene()
        {
            //SceneTransfer.instance.GotoScene(SceneName.Free);
        }
    }
}

